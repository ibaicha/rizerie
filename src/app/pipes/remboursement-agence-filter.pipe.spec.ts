import { RemboursementAgenceFilterPipe } from './remboursement-agence-filter.pipe';

describe('RemboursementAgenceFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RemboursementAgenceFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
